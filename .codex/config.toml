[mcp_servers.patternfly-mcp]
command = "podman"
args = [
    "run",
    "--rm",
    "-i",
    "--security-opt=no-new-privileges",
    "--cap-drop=ALL",
    "localhost/patternfly-mcp:latest",
    "--log-stderr",
]

[mcp_servers.patternfly-mcp.env]
PODMAN_USERNS = "keep-id"
